# 🚀 Простая инструкция по деплою на Netlify

## Вариант 1: Через Netlify CLI (РЕКОМЕНДУЕТСЯ - самый простой)

### Шаг 1: Установите Netlify CLI

```bash
bun add -g netlify-cli
```

### Шаг 2: Войдите в Netlify

```bash
netlify login
```

Откроется браузер - нажмите "Authorize"

### Шаг 3: Деплой

```bash
cd auto-survey-form
netlify deploy --prod
```

Команда спросит:
- **Create & configure a new site?** → YES
- **Team?** → выберите вашу команду
- **Site name?** → придумайте название (например: cars-from-europe)
- **Publish directory?** → введите: `.next`

### Шаг 4: Добавьте переменные окружения

После деплоя:

```bash
netlify env:set TELEGRAM_BOT_TOKEN "ваш_токен_от_BotFather"
netlify env:set TELEGRAM_CHAT_ID "ваш_chat_id"
```

### Шаг 5: Передеплой с переменными

```bash
netlify deploy --prod
```

### ✅ Готово!

Netlify покажет вам ссылку на сайт!

---

## Вариант 2: Через веб-интерфейс Netlify (дольше)

### Шаг 1: Подготовьте проект

1. Зайдите в папку проекта:
   ```bash
   cd auto-survey-form
   ```

2. Создайте ZIP архив ВСЕЙ папки `auto-survey-form`:
   - Правой кнопкой на папку → "Сжать в архив"
   - ИЛИ через терминал:
   ```bash
   cd ..
   zip -r auto-survey-form.zip auto-survey-form
   ```

### Шаг 2: Зайдите на Netlify

1. Откройте [https://www.netlify.com](https://www.netlify.com)
2. Зарегистрируйтесь или войдите

### Шаг 3: Деплой

1. Нажмите **"Add new site"** → **"Deploy manually"**
2. Перетащите ZIP архив в окно
3. Дождитесь окончания деплоя

### Шаг 4: Настройте переменные окружения

1. Откройте ваш сайт в Netlify
2. Перейдите в **Site settings** → **Environment variables**
3. Нажмите **Add a variable**

Добавьте ДВЕ переменные:

**Переменная 1:**
- Key: `TELEGRAM_BOT_TOKEN`
- Value: ваш токен от BotFather

**Переменная 2:**
- Key: `TELEGRAM_CHAT_ID`
- Value: ваш chat id (число)

4. Нажмите **Save**

### Шаг 5: Передеплой

1. Перейдите в **Deploys**
2. Нажмите **Trigger deploy** → **Deploy site**
3. Дождитесь завершения

### ✅ Готово!

---

## Вариант 3: Через GitHub (для продвинутых)

1. Создайте репозиторий на GitHub
2. Запушьте проект
3. Подключите репозиторий к Netlify
4. Netlify автоматически деплоит при каждом коммите

---

## ⚠️ ВАЖНО после деплоя:

1. **Проверьте переменные окружения** - без них форма не будет работать!
2. **Передеплойте** после добавления переменных
3. **Протестируйте форму** - заполните тестовую заявку
4. **Проверьте Telegram** - должно прийти сообщение

## 🔗 Что деплоить?

- **ВЕСЬ проект** `auto-survey-form` (всю папку целиком)
- НЕ только `.next`
- НЕ только `src`
- ВСЮ папку `auto-survey-form` со всеми файлами

## ❓ Проблемы?

Если что-то не работает:
1. Проверьте переменные окружения
2. Убедитесь что нажали START у бота
3. Передеплойте сайт после добавления переменных
