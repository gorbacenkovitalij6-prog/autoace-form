# 🔧 Решение проблем деплоя на Netlify

## Проект: AutoAce (autoace24)

### ✅ Правильная конфигурация:

**Build settings:**
```
Build command: bun install && bun run build
Publish directory: out
Functions directory: netlify/functions
```

**Environment variables (обязательно!):**
```
TELEGRAM_BOT_TOKEN = ваш_токен_от_BotFather
TELEGRAM_CHAT_ID = ваш_chat_id_от_userinfobot
```

---

## 🚨 Частые ошибки и решения:

### 1. ❌ Build Failed (Ошибка сборки)

**Симптомы:**
- Deploy status показывает красный крестик
- В логах: "Build failed" или "Command failed"

**Решения:**

**A. Проверьте Build Command:**
1. Зайдите: Site settings → Build & deploy → Build settings
2. Убедитесь что:
   - Build command: `bun install && bun run build`
   - Publish directory: `out`

**B. Проверьте Node версию:**
- В `netlify.toml` должно быть: `NODE_VERSION = "20"`

**C. Очистите кеш и пересоберите:**
1. Deploys → Trigger deploy → Clear cache and deploy site

---

### 2. ❌ Function Error (Форма не отправляет)

**Симптомы:**
- Форма заполняется, но после отправки показывает ошибку
- В консоли браузера: "Failed to fetch" или "500 error"

**Решения:**

**A. Проверьте переменные окружения:**
1. Зайдите: Site settings → Environment variables
2. Убедитесь что добавлены ОБЕ переменные:
   ```
   TELEGRAM_BOT_TOKEN
   TELEGRAM_CHAT_ID
   ```
3. Если их нет - добавьте!
4. После добавления: Deploys → Trigger deploy

**B. Проверьте правильность токена и Chat ID:**
- Token должен выглядеть как: `123456789:ABCdef...`
- Chat ID должен быть числом: `123456789`

**C. Проверьте что бот активирован:**
1. Найдите вашего бота в Telegram
2. Нажмите START
3. Попробуйте отправить форму снова

---

### 3. ❌ 404 Page Not Found

**Симптомы:**
- Сайт открывается, но показывает "Page not found"
- Или вообще не открывается

**Решения:**

**A. Проверьте Publish directory:**
1. Site settings → Build & deploy → Build settings
2. Publish directory должен быть: `out`

**B. Проверьте что сборка завершилась успешно:**
1. Deploys → последний деплой
2. Должен быть зеленый статус "Published"

---

### 4. ❌ Изображение логотипа не загружается

**Симптомы:**
- Вместо логотипа пустое место или broken image

**Решения:**

**A. Проверьте URL изображения:**
- Текущий URL: `https://i.ibb.co/WZMcVj0/Lucid-Origin-Premi.jpg`
- Откройте в браузере и убедитесь что картинка загружается

**B. Если картинка не открывается:**
1. Загрузите новую картинку на https://imgbb.com
2. Скопируйте прямую ссылку
3. Замените в коде: `src/app/page.tsx`

---

### 5. ❌ Telegram не получает сообщения

**Симптомы:**
- Форма успешно отправляется (показывает "Успешно!")
- Но сообщение не приходит в Telegram

**Решения:**

**A. Проверьте что используете правильный Chat ID:**
1. Откройте @userinfobot в Telegram
2. Напишите `/start`
3. Скопируйте ваш ID (число)
4. Сравните с Chat ID в Netlify переменных

**B. Проверьте что бот создан правильно:**
1. Откройте @BotFather
2. Напишите `/mybots`
3. Выберите вашего бота
4. API Token → скопируйте токен
5. Сравните с токеном в Netlify переменных

**C. Проверьте что START нажат:**
1. Найдите вашего бота по username
2. Нажмите START
3. Попробуйте отправить форму снова

---

## 🔍 Где смотреть логи:

### Deploy logs (логи сборки):
1. https://app.netlify.com/sites/autoace24/deploys
2. Кликнуть на последний деплой
3. Посмотреть "Deploy log"

### Function logs (логи отправки):
1. https://app.netlify.com/sites/autoace24/functions
2. Кликнуть на `submit`
3. Посмотреть "Function logs"

---

## ✅ Чек-лист для успешного деплоя:

- [ ] Build command: `bun install && bun run build`
- [ ] Publish directory: `out`
- [ ] Functions directory: `netlify/functions`
- [ ] NODE_VERSION = 20 в netlify.toml
- [ ] TELEGRAM_BOT_TOKEN добавлен в Environment variables
- [ ] TELEGRAM_CHAT_ID добавлен в Environment variables
- [ ] Бот создан через @BotFather
- [ ] Нажат START у бота
- [ ] Chat ID получен через @userinfobot
- [ ] После добавления переменных сделан Trigger deploy
- [ ] Deploy завершился успешно (зеленая галочка)
- [ ] Сайт открывается
- [ ] Форма отправляется
- [ ] Сообщение приходит в Telegram

---

## 🆘 Если ничего не помогло:

1. **Посмотрите точные логи ошибки:**
   - Deploy log (для ошибок сборки)
   - Function log (для ошибок отправки)
   - Console браузера (F12 → Console)

2. **Попробуйте редеплой с очисткой кеша:**
   - Deploys → Trigger deploy → Clear cache and deploy site

3. **Проверьте что репозиторий актуальный:**
   - https://github.com/gorbacenkovitalij6-prog/autoace-form
   - Должен содержать все файлы проекта

---

## 📞 Ссылки на проект:

- **GitHub:** https://github.com/gorbacenkovitalij6-prog/autoace-form
- **Netlify:** https://app.netlify.com/sites/autoace24
- **Live Site:** https://autoace24.netlify.app (или ваш кастомный домен)

---

## 🎯 Примеры успешных логов:

**Успешная сборка:**
```
12:34:56 PM: Build ready to start
12:34:57 PM: Installing dependencies
12:35:30 PM: Running build command
12:36:45 PM: Exporting static pages
12:37:20 PM: Build succeeded!
12:37:25 PM: Site is live ✨
```

**Успешная отправка формы:**
```
Function invocation complete
Status: 200
Duration: 1234ms
```
