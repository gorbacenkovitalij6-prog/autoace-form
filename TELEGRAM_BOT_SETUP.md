# 🤖 Настройка Telegram бота с WebApp

## Шаг 1: Создание бота

1. Откройте Telegram и найдите **@BotFather**
2. Отправьте команду `/newbot`
3. Введите имя бота (например: "Подбор Авто")
4. Введите username (должен заканчиваться на 'bot', например: `auto_podbor_bot`)
5. **Сохраните токен бота** (выглядит как `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

## Шаг 2: Получение Chat ID

1. Найдите в Telegram бота **@userinfobot**
2. Отправьте `/start`
3. Скопируйте ваш **Chat ID** (число)

## Шаг 3: Настройка переменных окружения

Откройте файл `.env.local` и добавьте:

```env
TELEGRAM_BOT_TOKEN=ваш_токен_от_BotFather
TELEGRAM_CHAT_ID=ваш_chat_id
```

## Шаг 4: Деплой сайта

Сначала нужно задеплоить ваш сайт, чтобы получить постоянный URL.

### Вариант А: Деплой на Netlify (рекомендуется)

1. Зарегистрируйтесь на [Netlify](https://www.netlify.com/)
2. Соберите проект: `bun run build`
3. Задеплойте папку `out` на Netlify
4. Добавьте переменные окружения в настройках Netlify:
   - `TELEGRAM_BOT_TOKEN`
   - `TELEGRAM_CHAT_ID`
5. Скопируйте URL вашего сайта (например: `https://your-app.netlify.app`)

### Вариант Б: Деплой на Vercel

1. Установите Vercel CLI: `bun add -g vercel`
2. Запустите: `vercel`
3. Следуйте инструкциям
4. Добавьте переменные окружения через `vercel env add`

## Шаг 5: Настройка WebApp кнопки в боте

### Простой способ (рекомендуется)

1. Откройте чат с вашим ботом
2. Отправьте BotFather команду `/setmenubutton`
3. Выберите вашего бота
4. Введите текст для кнопки (например: "🚗 Подобрать авто")
5. Введите URL вашего сайта (например: `https://your-app.netlify.app`)

Теперь у вашего бота появится кнопка Menu, которая открывает WebApp!

### Продвинутый способ (через код)

Создайте простого бота на Python или Node.js:

#### Python (с библиотекой python-telegram-bot)

```python
from telegram import Update, WebAppInfo, KeyboardButton, ReplyKeyboardMarkup
from telegram.ext import ApplicationBuilder, CommandHandler, ContextTypes

async def start(update: Update, context: ContextTypes.DEFAULT_TYPE):
    keyboard = [
        [KeyboardButton(
            text="🚗 Подобрать автомобиль",
            web_app=WebAppInfo(url="https://your-app.netlify.app")
        )]
    ]
    reply_markup = ReplyKeyboardMarkup(keyboard, resize_keyboard=True)

    await update.message.reply_text(
        'Нажмите кнопку ниже, чтобы начать подбор автомобиля!',
        reply_markup=reply_markup
    )

app = ApplicationBuilder().token("ВАШ_ТОКЕН_БОТА").build()
app.add_handler(CommandHandler("start", start))
app.run_polling()
```

#### Node.js (с библиотекой node-telegram-bot-api)

```javascript
const TelegramBot = require('node-telegram-bot-api');

const token = 'ВАШ_ТОКЕН_БОТА';
const bot = new TelegramBot(token, { polling: true });

bot.onText(/\/start/, (msg) => {
  const chatId = msg.chat.id;

  bot.sendMessage(chatId, 'Нажмите кнопку ниже, чтобы начать подбор автомобиля!', {
    reply_markup: {
      keyboard: [
        [
          {
            text: '🚗 Подобрать автомобиль',
            web_app: { url: 'https://your-app.netlify.app' }
          }
        ]
      ],
      resize_keyboard: true
    }
  });
});
```

## Шаг 6: Тестирование

1. Найдите вашего бота в Telegram
2. Отправьте `/start`
3. Нажмите на кнопку "🚗 Подобрать автомобиль"
4. Форма откроется как WebApp
5. Ваши данные (имя, username) автоматически подтянутся
6. Заполните форму и отправьте
7. Проверьте, что заявка пришла вам в Telegram

## 🎯 Преимущества WebApp

✅ Автоматическое получение данных пользователя
✅ Нативный интерфейс внутри Telegram
✅ Не нужно вводить контактные данные
✅ Безопасная идентификация пользователя
✅ Удобство для пользователей

## ⚠️ Важно

- URL должен быть **HTTPS** (не HTTP)
- Домен должен быть доступен публично
- Тестировать на localhost не получится - нужен реальный деплой
- Бот должен быть запущен (`/start`)

## 🔧 Устранение проблем

### WebApp не открывается
- Проверьте, что URL правильный и доступен
- URL должен быть HTTPS
- Попробуйте открыть URL в браузере

### Данные пользователя не приходят
- Проверьте, что открыли через Telegram WebApp
- Проверьте консоль браузера на ошибки
- Убедитесь, что Telegram Web App SDK загружен

### Сообщения не приходят в Telegram
- Проверьте переменные окружения на сервере
- Убедитесь, что нажали Start у бота
- Проверьте логи на сервере
