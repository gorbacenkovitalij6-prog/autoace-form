# 🔧 Что было исправлено

## Проблема

Netlify не мог собрать проект, выдавая ошибку:
```
Could not find a "pages" or "app" directory in the project root
```

## Причина

Netlify пытался собрать проект из корня репозитория, но сам Next.js проект находится в папке `auto-survey-form`.

## Решение

Обновлен файл `netlify.toml`:

### Было:
```toml
[build]
  command = "npm run build"
  publish = ".next"
```

### Стало:
```toml
[build]
  base = "auto-survey-form"        ← указали базовую директорию
  command = "bun install && bun run build"
  publish = ".next"
```

## Что изменилось

1. **Добавлено `base = "auto-survey-form"`**
   - Теперь все команды сборки выполняются из папки `auto-survey-form`
   - Netlify автоматически находит там проект Next.js

2. **Обновлена команда сборки**
   - Было: `npm run build`
   - Стало: `bun install && bun run build`
   - Сначала устанавливаются зависимости, потом сборка

3. **Обновлена версия Node.js**
   - Было: Node 18
   - Стало: Node 20 (более актуальная)

## Как это работает

```
Ваш проект/
└── auto-survey-form/          ← Netlify начинает отсюда
    ├── src/
    │   └── app/               ← Next.js находит эту папку
    ├── netlify.toml
    └── package.json
```

Netlify:
1. Открывает ваш ZIP архив
2. Видит `base = "auto-survey-form"` в `netlify.toml`
3. Переходит в папку `auto-survey-form`
4. Выполняет `bun install && bun run build`
5. Публикует `.next` папку

## Что нужно сделать

**Ничего!** Всё уже исправлено.

Просто:
1. Создайте ZIP всего проекта (с папкой `auto-survey-form` внутри)
2. Загрузите на Netlify
3. Добавьте переменные окружения
4. Сделайте Trigger deploy

См. подробную инструкцию: **NETLIFY_DEPLOY.md**

## Проверка

После деплоя проверьте в логах Netlify:

✅ Хорошо:
```
12:34:56 PM: Starting build
12:34:57 PM: Detected base directory: auto-survey-form
12:34:58 PM: Installing dependencies...
12:35:30 PM: Building Next.js...
12:36:45 PM: Build succeeded!
```

❌ Плохо:
```
Could not find a "pages" or "app" directory
```

Если видите ошибку - убедитесь что:
- В ZIP есть папка `auto-survey-form`
- В ней есть файл `netlify.toml`
- После изменений сделали Trigger deploy
