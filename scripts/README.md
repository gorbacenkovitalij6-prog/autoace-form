# 📝 Скрипты для постинга в Telegram канал

Готовые скрипты для добавления автомобилей в ваш канал с кнопкой WebApp.

## 🐍 Python версия

### Установка:

```bash
pip install -r requirements.txt
```

### Настройка:

Откройте `post_to_channel.py` и замените:

```python
BOT_TOKEN = "ваш_токен_от_BotFather"
CHANNEL_ID = "@ваш_канал"
WEBAPP_URL = "https://ваш-сайт.netlify.app"
```

### Запуск:

```bash
python post_to_channel.py
```

## 📦 Node.js версия

### Установка:

```bash
npm install
```

### Настройка:

Откройте `post_to_channel.js` и замените:

```javascript
const BOT_TOKEN = 'ваш_токен';
const CHANNEL_ID = '@ваш_канал';
const WEBAPP_URL = 'https://ваш-сайт.netlify.app';
```

### Запуск:

```bash
npm run post
```

## 🚀 Что умеют скрипты

1. **Отправка поста с автомобилем** - добавляет фото, описание и кнопку WebApp
2. **Создание закрепленного сообщения** - делает постоянную кнопку вверху канала
3. **Массовая отправка** - можно отправить несколько автомобилей подряд

## 📝 Пример использования

### Отправить один автомобиль:

```python
car_data = {
    "brand": "Toyota",
    "model": "Camry",
    "year": 2022,
    "transmission": "Автомат",
    "engine": "2.5L Бензин",
    "color": "Черный",
    "mileage": "25 000",
    "price": "2 500 000",
    "description": "✨ Состояние отличное",
    "location": "Москва",
    "photo_url": "https://example.com/photo.jpg"
}

asyncio.run(post_car_to_channel(car_data))
```

## ⚙️ Настройка бота

1. Создайте бота через @BotFather
2. Добавьте бота в канал как администратора
3. Дайте права на отправку сообщений
4. Скопируйте токен и вставьте в скрипт

## 📚 Дополнительно

См. [CHANNEL_INTEGRATION.md](../CHANNEL_INTEGRATION.md) для подробной документации.
